{
  "name": "elysia-test",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir ./build",
    "start": "bun start ./build/index.js"
  },
  "dependencies": {
    "elysia": "latest",
    "prettier": "^3.0.3"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
